import React from 'react';
import { render } from 'react-dom';
import TicketInfo from './pages/TicketInfo';

render(<TicketInfo />, document.getElementById('root'));

// if (module.hot) {  
//   module.hot.accept();
// }